import React from 'react';
import NavBar from './layout/NavBar';
import SideBar from './layout/SideBar';

function Home(props) {
    return (
    <div>
        <NavBar/>
        <SideBar/>
    </div>
    );
}

export default Home;